{
  "name": "isaac-character-studio",
  "version": "0.8.0",
  "description": "The Binding of Isaac Rebirth Character & Mod Workshop (DLC Compatible)",
  "main": "server/index.js",
  "type": "module",
  "author": "Isaac Character Studio",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "node server/index.js",
    "client": "npm --prefix client run dev",
    "start": "node server/index.js",
    "build": "npm --prefix client run build",
    "build:client": "npm --prefix client run build",
    "build:database": "node scripts/buildDatabase.js",
    "pack": "node scripts/packZip.js",
    "test": "node test/verifyAssetEngine.js && node test/verifyCharacterCompile.js && node test/verifyServerHttp.js && node test/verifyDualAndForgottenCompile.js && node test/verifyGreedCoopAndDualPools.js"
  },
  "dependencies": {
    "archiver": "^7.0.1",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "fs-extra": "^11.2.0",
    "multer": "^1.4.5-lts.1",
    "pngjs": "^7.0.0",
    "xml2js": "^0.6.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "esbuild": "^0.25.0",
    "sharp": "^0.35.4"
  }
}
